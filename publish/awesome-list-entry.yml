# 收录条目 —— 提交到 awesome-dsh-plugin 仓库时，放到：
#   data/plugins/Yu-tao-Li__dsh-read-image-view.yml
# 注意：英文描述含 ": "（冒号+空格），按规则必须加引号。
url: https://github.com/Yu-tao-Li/dsh-read-image-view
name: Yu-tao-Li/dsh-read-image-view
category: tools
description:
  en: 'For displaying the images read in the conversation (read_image results) in the DSH Web GUI: a Read image row with a default thumbnail and an in-page full-resolution lightbox (zoom buttons, mouse wheel, 1:1 original size), plus the metadata envelope.'
  zh: 用于展示对话中读取的图片（read_image 的结果）：在 DeepSeek Harness Web GUI 对话流中以专用 Read image 行呈现，默认缩略图 + 页面内全精度放大层（缩放按钮/滚轮/1:1 原始尺寸）+ 元数据信封。
